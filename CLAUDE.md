Always keep the code clean and readable
Keep the code maintainable
Respect the Page object model for every module and make locator changes in page class only 
Always perfer get_by_Role,get_by_label,get_by_text before using any css and xpath locators
Always make reusable components which are common for multiple modules like navbar logout button etc these are just some ideas 
Keep the main mindset to test the application against industry standards not just filling forms and clicking button 
Be very vigilante with API calls to validate the data is accurately displayed to the user
Give security tests equal importance as functional tests